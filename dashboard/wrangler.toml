name = "domains-dashboard-api"
main = "api-worker.js"
compatibility_date = "2023-12-01"

# Environment variables (set via wrangler secret put)
# CLOUDFLARE_API_TOKEN
# NETLIFY_ACCESS_TOKEN  
# GITHUB_TOKEN

[env.production]
vars = { ENVIRONMENT = "production" }

[env.development]
vars = { ENVIRONMENT = "development" }