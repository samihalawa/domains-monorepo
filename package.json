{
  "name": "domains-monorepo",
  "version": "1.0.0",
  "description": "Domain management platform with intelligent dashboard",
  "private": true,
  "scripts": {
    "deploy": "cd workers/dashboard-api && wrangler deploy",
    "dev": "cd workers/dashboard-api && wrangler dev"
  },
  "workspaces": [
    "sites/*"
  ]
}