name = "payments-sumup"
main = "index.js"
compatibility_date = "2024-03-01"

# Example route. You can also bind via a custom domain or subdomain.
# routes = [
#   { pattern = "payments.yourdomain.com/api/sumup-checkout", zone_name = "yourdomain.com" }
# ]

# Required secrets (set with `wrangler secret put`):
# - SUMUP_SECRET_API_KEY
# - SUMUP_MERCHANT_CODE
# - SUMUP_PAY_TO_EMAIL
# - SUMUP_RETURN_URL

